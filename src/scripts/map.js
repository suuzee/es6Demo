export default {
    getKeys (map = new Map) {
        return map.keys();
    },
    getValues (map = new Map) {
        return map.values();
    },
    getEntries (map = new Map) {
        return map.entries();
    }
}
